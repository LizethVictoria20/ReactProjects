import RenderAPI from "./RenderAPI";
function Detail() {
  const { characters } = RenderAPI(
    "https://stand-by-me.herokuapp.com/api/v1/characters"
  );
  const images = "https://jojos-bizarre-api.netlify.app/assets/";
  return (
    
  )
}

export default Detail;
